; = fib BLOCK
	: IF (< n 2)
		: n
		; = n (- n 1)
		; EVAL (+ (+(+ "=_i" i) " ") n)
		; = i (+ 1 i)
		; = tmp (CALL fib)
		; = i (- i 1)
		; = n (EVAL (+ "_i" i))
		: + tmp (CALL fib)
; = i 0
; = n 10
: OUTPUT CALL fib
3
